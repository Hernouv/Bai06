#include <bits/stdc++.h>
using namespace std;

void not_main()
{
}

int32_t main()
{
    cin.tie(0)->sync_with_stdio(false);
    if (fopen("DOUBLESEA.INP", "r"))
    {
        freopen("DOUBLESEA.INP", "r", stdin);
        freopen("DOUBLESEA.OUT", "w", stdout);
    }
    not_main();
    return 0;
}