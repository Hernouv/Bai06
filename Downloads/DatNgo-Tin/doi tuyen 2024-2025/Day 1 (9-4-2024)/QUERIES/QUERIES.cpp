#include <bits/stdc++.h>
using namespace std;

void not_main()
{
}

int32_t main()
{
    cin.tie(0)->sync_with_stdio(false);
    if (fopen("QUERIES.INP", "r"))
    {
        freopen("QUERIES.INP", "r", stdin);
        freopen("QUERIES.OUT", "w", stdout);
    }
    not_main();
    return 0;
}