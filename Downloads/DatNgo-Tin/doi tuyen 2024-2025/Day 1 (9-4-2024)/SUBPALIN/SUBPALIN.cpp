#include <bits/stdc++.h>
using namespace std;

typedef pair<int, int> ii;
#define fi first
#define se second

const int nmax = 2e4 + 10;
int n, q;
char str[nmax];

void not_main()
{
    cin >> n >> q;
    for (int i = 1; i <= n; ++i)
        cin >> str[i];
    for (int i = 1; i <= q; ++i)
    {
    }
}

int32_t main()
{
    cin.tie(0)->sync_with_stdio(false);
    if (fopen("SUBPALIN.INPUT", "r"))
    {
        freopen("SUBPALIN.INPUT", "r", stdin);
        freopen("SUBPALIN.OUTPUT", "w", stdout);
    }
    not_main();
    return 0;
}