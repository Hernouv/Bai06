#include <bits/stdc++.h>
using namespace std;

void not_main()
{
}

int32_t main()
{
    if (fopen("STRING.INP", "r"))
    {
        freopen("STRING.INP", "r", stdin);
        freopen("STRING.OUT", "w", stdout);
    }
    not_main();
    return 0;
}