#include <bits/stdc++.h>
using namespace std;

int demuoc(long inp)
{
	int temp = 0;
	for (long i = 0; i < sqrt(inp); ++i)
	{
		if (inp)
	}
}

int main()
{
	long n;
	cin >> n;
	int out = demuoc(n);
}

