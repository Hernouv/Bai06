#include <bits/stdc++.h>
using namespace std;

int prevmax = 0;
int arr[5001], dp[5001], dpsum[5001];

int main()
{
    cin.tie(0)->sync_with_stdio(false);
    // freopen("SUMNT.INP", "r", stdin);
    // freopen("SUMNT.OUT", "w", stdout);
    int n;
    cin >> n;

    return 0;
}