#include <bits/stdc++.h>
using namespace std;

int num_cnt[2501], dp[2501], arr[50];

int main()
{
    cin.tie(0)->sync_with_stdio(false);
    // freopen("EXP.INP", "r", stdin);
    // freopen("EXP.OUT", "w", stdout);
    int n, rq_sum;
    cin >> n;
    for (int i = 1; i <= n; ++i)
        cin >> arr[i];

    return 0;
}